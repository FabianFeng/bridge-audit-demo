# 桥梁工程设计文件智能审校系统

> 让 200 页施工图总说明的初审从 5 天压缩到 10 分钟。

---

## 一、客户场景与痛点

公路 / 桥梁工程设计单位每出一份施工图总说明（典型 150–250 页），都要走一遍**院级 / 专家组审校**。当前流程的真实痛点：

1. **抄稿污染** — 设计文件大量从历史项目复制改写。改不干净就会留下"江苏宿迁"出现在浙江项目里、"灵山港 2 号桥"在不同章节是"连续刚构"又是"连续箱梁"。这类问题人眼一行行翻几乎查不出来。
2. **规范版本过时** — 行业规范每 3–5 年更新一次（JT/T 722-2008 → 2023，JTG D63-2005 → JTG/T 3365-04-2020）。文件里引用旧版需要现场比对国家标准化信息网。
3. **设计参数不自洽** — 同一座桥的全长、跨径、桩长，在桥梁布设表 / 详述章节 / 变化说明三处常出现数值矛盾。
4. **专业取值偏差** — 大桥设计使用年限应≥100 年、桥面板混凝土应≥C40、T 梁不应配盆式支座…… 专家凭经验记得，新人靠手册查。
5. **审校周期** — 200 页文件，一位资深审校工程师**全功能审一遍需要 3–5 个工作日**。

院里的资深审校工程师时薪 ¥300–500。一份文件初审人力成本 ¥10,000–20,000，且时间常常拖延整个交付节奏。

---

## 二、我们的方案 — 一句话

**12 条确定性规则 + 大模型语义审查 + 跨章节一致性比对**，三层并行，10 分钟内输出 100–200 条候选问题，每条带「位置 · 原文 · 规范依据 · 现状 · 修改建议」四段式结构，**直接交给审校工程师复核**。

不是替代人审，是把审校工程师从"逐行翻找问题"解放成"复核 AI 给的清单"。**人审环节省时 60–80%**。

### 真实跑通数据（演示环境）

| 项目 | 数据 |
|---|---|
| 输入 | 甬金衢上高速公路施工图总说明 PDF 41MB / 约 150 页 |
| OCR | 约 3 分钟（GPU 加速，MinerU pipeline + 表格识别） |
| 审校 | 约 3 分钟（41 章节并行 + 全文跨章节比对） |
| 端到端 | **约 7 分钟** |
| 候选问题数 | 144 条 |
| 命中维度 | 5 大维度全覆盖 |

---

## 三、产品能力清单（与客户审校五大维度逐条对应）

客户既有审校体系明确列出**五大维度**，我们的方案逐条对齐：

### ① 参考规范有效性
- **机制**：本地维护一份《规范状态库》（含 30+ 条主流公路桥梁规范的现行 / 替代关系），文件中每条规范引用 → 正则提取 → 比对状态库 → 命中已废止/已替代立刻标红
- **示例输出**："JT/T 722-2008 已被 JT/T 722-2023 替代"
- **可扩展**：规范库可对接国家工程建设标准化信息网（csres.com）做季度自动同步

### ② 涉及规范详细条文的，审查与有效规范符合性
- **机制**：System Prompt 里硬编码关键条款（设计使用年限 / 环境类别细分 / 主梁钢材牌号 / 桥面板混凝土等级 / 防腐涂层年限 / T 梁支座型式 / 温度梯度），大模型对照文档逐项核查
- **同时**规则层做硬约束（特大桥年限<100 年立刻报 high，环境类别出现"Ⅰ-A"立刻报）
- **示例输出**："本项目含特大桥、大桥，主体结构设计使用年限应为 100 年（JTG 2120-2020 表 3.3.1），文档声明 50 年 — 建议改为 100 年"

### ③ 不同部位的相同设计在表述、技术要求不一致
- **机制**："设计基线"先抽取（项目身份 / 设计标准 / 几何尺寸 / 主要材料 / 引用规范 / 桥梁清单），然后每章节送 LLM 对照基线审查 — 同一类构件（如 T 梁、桩基、桥面板）在不同章节的技术要求会被自动比对
- **示例输出**："灵山港 2 号桥在基线桥梁清单中为'预应力砼变截面连续箱梁'；第 X 章描述为'预应力混凝土变截面连续刚构桥' — 上下部结构形式描述不一致"

### ④ 相同内容前后不一致 / 数据不统一
- **机制**：**跨章节比对**专项任务，让大模型在抽完基线后，把全文（最多 130K 字符）作为一次性输入，让它**逐项对照基线与全文中出现的桥梁数据 / 设计参数 / 项目地名**
- **示例输出**："桥梁布设表：A 桥全长 439.04m；详述章节：A 桥全长 536m — 同一对象数值矛盾"

### ⑤ 设计说明中出现其它项目名称 / 与本项目所在省份地市不一致的文字
- **机制**：基线里强制包含"项目身份"（项目名 / 省 / 市 / 县区）；System Prompt 把这条标为**最高优先级**："本项目在浙江金华，文中如出现'东阳市'、'上海'、'江苏'、'安徽'等非本项目所在地的地名 → 必须 high 级报出"
- **真实命中**：在 144 条 finding 里，**「项目身份」类 27 条**、**「跨章节」与「数据不统一」累计 32 条** — 这两类是 RAG 拿不到的

---

## 四、关键技术决策 — 为什么不用 RAG

很多团队上来就上 RAG（检索增强生成）。我们**明确选择不走 RAG**，原因：

| 任务特征 | RAG | 我们的方案 |
|---|---|---|
| 输入特征 | 大量文档库，按问题检索片段 | 单份文件，**整文档审查** |
| 任务本质 | 问答（找答案） | 一致性检查（找矛盾） |
| 上下文需要 | 局部片段够用 | **必须看全文** — 矛盾分散在不同章节 |
| 切片影响 | 必要（向量库） | 致命 — 切片就丢了"前后对照"的能力 |

**举个具体例子**：

> 文档第 12 页：「灵山港 2 号桥跨径布置为 60+100+60」  
> 文档第 89 页：「灵山港 2 号桥跨径布置为 50+80+50」

RAG 方案下，用户得问对问题（比如"灵山港 2 号桥跨径多少？"），系统才会去检索。即使检索到了，也只会拿其中一个片段回答。**它不会主动告诉你这两处矛盾。**

我们的方案：抽完基线（包含"灵山港 2 号桥跨径 60+100+60"）→ 全文跨章节比对 → **主动**抛出"第 89 页与基线不符"的 finding。

**结论**：审校的本质是"找矛盾"，不是"找答案"。这件事 RAG 做不了，必须让模型看完整上下文。

---

## 五、上下文管理 — 为什么能看完整文档

### 上下文窗口

- 当前部署模型：**Gemma-3-31B（NVFP4 量化）**
- 上下文长度：**131,072 tokens** ≈ 约 95,000 中文字符 ≈ 一份完整施工图总说明
- 跨章节比对任务保守截到 130K 字符（≈ 97K tokens），留 8K 给系统提示 + 8K 给输出窗口

### 分层调度策略

```
全文 ─┬─→ 规则层（毫秒级，正则）          ──→ 命中即报
      │
      ├─→ 前部 60K 字符 → 抽取「设计基线」  ──→ 紧凑结构化摘要
      │                          │
      │                          ├─→ 每章节并行送 LLM
      │                          │   （5 路并发 + 信号量控制 GPU 压力）
      │                          │
      │                          └─→ 基线 + 全文 130K → 跨章节比对
      │
      └─→ SSE 流式 推送  ────────→ 前端实时展示，用户不用等
```

**重点**：
- 章节并行（41 章节同时跑）让用户**不用等全文跑完**才看到第一个问题
- 信号量限制并发（5 路）防止 GPU 显存爆炸
- 三层结果合并去重 — 规则层抓住的低级笔误，LLM 不再重复报

---

## 六、产品形态

### 用户体验

1. 上传 PDF / Word / Markdown / TXT（拖拽即可）
2. 后端自动判断格式，PDF 走 MinerU OCR（layout 分析 + PaddleOCR + 表格识别）
3. 审校实时流式输出，每发现一个问题立刻显示卡片
4. 卡片可按 严重 / 中等 / 轻微 筛选，按章节定位原文（高亮跳转）
5. **历史记录持久化** — 关掉浏览器再打开，之前跑过的审校结果直接重看，不用重跑
6. 全程页面底部计时条显示每个阶段耗时

### 部署形态

**完全私有部署，数据不出企业网**：
- 后端 FastAPI + 前端纯 HTML（无外部依赖）
- LLM 走本地 vLLM（OpenAI 兼容协议），模型权重在企业 GPU 服务器
- OCR 走本地 MinerU（pipeline 模式，模型权重已下载）
- 兼容任意 OpenAI 兼容端点（vLLM / OpenRouter / 自建 API）

**硬件门槛**：
- 推荐：2 × RTX 5090 (32GB) 或 1 × A100 80GB（vLLM + MinerU 共用）
- 最低：1 × RTX 4090 24GB（需用更小量化模型，如 Qwen2.5-14B）

### 数据安全

- 文件上传到本地临时目录 `/tmp/bridge_audit/`
- 历史结果存项目目录 `audits/`（可配置）
- **全程不联网**，不调用任何外部 API
- 审校结果 JSON 化存盘，可对接企业 OA / 项目管理系统

---

## 七、与人工审校协同的工作流建议

我们**不主张取代人审**。建议接入工作流：

```
设计成稿
    │
    ├─→ AI 审校（5–10 分钟）           ──→ 100–200 条候选问题
    │
    └─→ 资深审校工程师复核（0.5–1 天）  ──→ 确认 / 排除 / 补充
                │
                ├─→ 设计修改
                │
                └─→ 二审签发
```

**节省点**：
- 工程师不再"翻 200 页找问题"，而是"复核清单上的候选项"
- AI 一致性比对（维度 ③④⑤）是工程师的肉眼盲区
- 单份文件初审从 3–5 天 → **0.5–1 天**

**保留人审的环节**：
- 设计合理性的工程判断
- 与上位规划 / 专家批复的对照（这类外部依据 AI 没数据）
- 责任签字与最终决策

---

## 八、可对接的扩展能力

| 能力 | 现状 | 扩展 |
|---|---|---|
| 规范库 | 内置 30+ 条公路桥梁规范 | 季度同步 csres.com（国家工程建设标准化信息网） |
| 审校规则 | 12 条领域规则 | 按项目类型扩充（高速 / 一级 / 二级公路）；接入院内既有审校 checklist |
| 报告输出 | 实时 SSE + JSON | 一键导出 Word / PDF 审校意见书（含截图 + 修改批注） |
| 历史与协同 | 单机持久化 | 多用户登录、按项目归档、按章节分派复核人 |
| 模型 | Gemma-3-31B | 可替换 Qwen3 / DeepSeek-V3 / 行业微调模型 |
| OCR | MinerU 通用 | 可针对设计院图纸做表格识别微调 |

---

## 九、商务节奏建议

### POC（4 周）
- W1：在客户院内 GPU 服务器部署，跑通既有 3 份施工图总说明
- W2：核对 AI 命中与院内审校结论的差异，调参 prompt / 规则
- W3：UI 与院内既有系统对接（账号 / 项目管理）
- W4：用户验收 + 培训

### 试点（3 个月）
- 选 1 个事业部 / 项目组日常使用
- 收集 50+ 份真实文件的命中数据
- 持续优化规则库 + prompt

### 推广
- 院级铺开 / 同类设计院推广
- 行业规范库做成可订阅 SaaS（仅规范库联网，文件审校仍在企业内网）

---

## 十、为什么是我们

- **端到端工程能力**：从 PDF OCR → LLM 调度 → 前端实时展示 → 历史持久化，全部自研、可控、可调
- **领域沉淀**：12 条规则的每一条都对应一处真实痛点（设计使用年限 / 环境类别 / 主梁钢材 / 防腐年限 / T 梁支座 / 温度梯度 / 桩长自洽），不是空想的 demo
- **演示可见**：演示地址 http://218.22.75.175:8088 ，代码完全开源在 https://github.com/FabianFeng/bridge-audit-demo
- **真实跑过**：当前 demo 在客户提供的真实 41MB / 150 页文件上跑出过 144 条 finding，5 大维度全命中

---

## 联系

**演示地址**：http://218.22.75.175:8088  
**代码仓库**：https://github.com/FabianFeng/bridge-audit-demo  
**联系人**：Fabian
